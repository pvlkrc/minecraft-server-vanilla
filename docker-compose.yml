services:
  muj-server:
    image: pavelkracmar122/pavel-mc-server-vanilla:latest
    container_name: minecraft-server-vanilla
    restart: unless-stopped
    ports:
      - "25565:25565"
    environment:
      - EULA=true
    volumes:
      - ./data:/minecraft/game-conf