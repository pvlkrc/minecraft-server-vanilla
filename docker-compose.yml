services:
  minecraft-server:
    image: ghcr.io/pvlkrc/minecraft-server-vanilla:latest
    container_name: minecraft-server-vanilla
    restart: unless-stopped
    ports:
      - "25565:25565"
    environment:
      - EULA=true
    volumes:
      - ./data:/minecraft/game-conf